# Daily Report — 2026-04-18

## Summary
Added multi-provider AI extraction pipeline for PDF takeoffs and overhauled the main UI pages. Sidebar retired in favour of a reworked navbar-driven layout.

## New features
- **AI extraction API** — `app/api/extract/route.ts` accepts a PDF upload and routes it to the selected provider (query/header/form/env). Node runtime, 300s max.
- **Provider adapters** — `lib/quote/providers/` with `openai.ts`, `anthropic.ts`, `gemini.ts`, and a shared `types.ts` (discriminated `ExtractProvider` contract).
- **Quote domain layer** — new `lib/quote/` module: `pricing.ts`, `schema.ts`, `store.ts`, `types.ts`.
- **Profile page** — new `app/profile/page.tsx`.

## Dependencies
Added to `package.json`:
- `@anthropic-ai/sdk ^0.90.0`
- `@google/genai ^1.50.1`
- `openai ^6.34.0`

## UI / UX changes
- **Dashboard** (`app/dashboard/page.tsx`) — heavy rewrite (+563/-… lines).
- **Landing** (`app/page.tsx`) — large content/layout refresh.
- **Quote page** (`app/quote/page.tsx`) — major rework of the editor/pricing flow.
- **Processing, Projects, Settings pages** — rewritten for the new layout.
- **Auth page** — minor updates.
- **Navbar** — expanded; **MobileNav** reworked; **Sidebar removed**.
- **UploadModal** — tweaked to fit the new flow.
- **Footer, Animate** — small touch-ups.
- **globals.css, layout.tsx** — styling + layout adjustments.

## i18n
- `lib/i18n/en.ts` and `lib/i18n/fr.ts` updated for the new copy (FR had the larger update).

## Other
- `lib/suppliers.ts` updated.
- `lib/generateQuotePdf.ts` small fix.
- `next.config.ts` tweak.
- `bun.lock` now tracked.

## Stats
21 modified files, several new directories, **+2118 / -1669** lines (excluding new files).
